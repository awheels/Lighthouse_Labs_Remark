<div class="col-md-4 col-md-offset-4">
  <h1>Login</h1>
  <hr/>
  <% if @user.errors.any? %>
    <div>
      <strong>You have failed to meet the requirements for your login credentials, please fix them:</strong>
      <ul>
        <% @user.errors.full_messages.each do |user| %>
          <li><%= user %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <form role="form" method="post" action="/login">
    <label>Username:</label>
    <input type="text" class="form-control" name="username" value="<%= @user.username %>">
    <label>Password:</label>
    <input type="password" class="form-control" name="password" value="<%= @user.password %>">
    <br>
    <div><input type="submit" class="btn btn-info" value="Submit"></div>
  </form>
</div>

<div class="col-md-4 col-md-offset-4">
  <h1>Sign Up</h1>
  <hr/>
  <% if @user.errors.any? %>
    <div>
      <strong>Please fix the following errors:</strong>
      <ul>
        <% @user.errors.full_messages.each do |user| %>
          <li><%= user %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <form role="form" method="post" action="/signup">
    <label>Username:</label>
    <input type="text" class="form-control" name="username" value="<%= @user.username %>">
    <label>Email(optional):</label>
    <input type="text" class="form-control" name="email" value="<%= @user.email %>">
    <label>Password:</label>
    <input type="password" class="form-control" name="password" value="<%= @user.password %>">
    <input type="password" class="form-control" name="password" value="<%= @user.password %>">
    <br>
    <div><input type="submit" class="btn btn-info" value="Submit"></div>
  </form>

  <p> Do you already have an account? You can <a href='/login'>LOGIN</a> here. </p>
</div>